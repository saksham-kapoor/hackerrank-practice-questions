/*

string catAndMouse(int x, int y, int z) {
    int diff_x = z - x;
    int diff_y = z - y;
    if(diff_x < 0) {
        diff_x *= -1;
    }
    if(diff_y < 0) {
        diff_y *= -1;
    }
    if(diff_x < diff_y){
        return "Cat A";
    } else if (diff_x > diff_y) {
        return "Cat B";
    }  else {
        return "Mouse C";
    }
}

*/